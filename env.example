# Project Configuration
PROJECT_ENV=development

# Data Provider API Keys (never commit actual keys!)
MARKET_DATA_API_KEY=your_key_here
MEMORY_PRICE_API_KEY=your_key_here
KOREA_TRADE_API_KEY=your_key_here

# Database (if used)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=semis_alpha
DB_USER=user
DB_PASSWORD=password

# MLflow (if used)
MLFLOW_TRACKING_URI=http://localhost:5000

# API Configuration
API_PORT=8000
API_HOST=0.0.0.0
